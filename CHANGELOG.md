# frozen_string_literal: true

## [0.1.0] - 2026-09-18

- Initial release: configurable event-based notifications (Slack, Email,
  Webhook + custom providers), global/per-provider enable flags,
  per-provider event allow-lists, handlers, failure isolation, logging,
  Rails Railtie, RSpec suite.
